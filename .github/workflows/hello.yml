name: Mi primer workflow

on: [push]
   
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    name: imprimir mensaje
    run: echo "hola mundo cdsif"

name: If develop branch
if: github.ref == 'refs/heads/develop'
run: echo "This is the develop branch!"